# 発表前日の手動チェックリスト

`docs/requirements.md` §19 で「必須（発表前日）」とされる手動確認の記録用紙。
自動テストで担保できない範囲（**実レジストリへの副作用を伴う操作**・**他チームとの相互移管**・
**実ブラウザのパスキー**・**本番 URL での通し**）をここで潰す。

- 実施日: 2026-08-27（発表は 8/28）
- 実施者: 佐々木 琢登 / 星 はるか / 上原 拓也
- 対象: 本番 `https://dopamin.ut42tech.com`（API は `https://dopamin-api.ut42tech.com`）
- 関連: [`../testing.md`](../testing.md)（自動テストの実行方法）/ requirements §3.3・§11.6・§19・§21.2 #17

## 使い方

- 「結果」は `OK` / `NG` / `-`（未実施）で埋める。NG は必ず **issue 番号**を書く。
- 実レジストリを触る項目は**副作用がある**（実データが登録・削除される）。
  `docs/testing.md` §2 の注意を読んでから実施すること。
- 5〜7 は他チームとの調整が要る。**先に相手と時間を合わせてから**着手する。

---

## 1. 事前確認（全員・5 分）

| # | 項目 | 期待 | 担当 | 結果 | メモ |
|---|---|---|---|---|---|
| 1-1 | `main` の CI が green | `CI / check` と `e2e` がどちらも成功 | | | |
| 1-2 | `main` の Deploy が green | `Deploy API` / `Deploy Web` がどちらも成功 | | | |
| 1-3 | `GET /api/v1/health` | `status: "ok"`、両レジストリ `ok: true`、`db.ok: true` | | | |
| 1-4 | 本番が実 API モード | 未ログインで `/dashboard` を開くと `/login?next=/dashboard` へ 307。**200 が返ったら mock モードなので即エスカレーション**（`NEXT_PUBLIC_API_MODE`。#180） | | | |
| 1-5 | マイグレーション適用済み | `pnpm db:migrate` を当てた後に本番 DB のテーブルが最新（CI では自動適用しない。§16.2） | | | |
| 1-6 | 未ログインのお試しスコア | ランディング（`/`）の入力欄に `gogle` を入れるとスコアが出る（`POST /api/v1/uniqueness/preview`・認証不要）。**11 回目以降は 429**（IP 単位で毎分 10 回。仕様どおり） | | | |

## 2. エラーシミュレーション（§11.6・GMO 要件）

`MOCK_REGISTRY_FAIL_MODE` の 4 種と、実レジストリ URL を無効値にする方法の**両方**で確認する。
確認項目 (a)〜(e) は §11.6 のとおり。

### 2-A. `mock` アダプタ（`REGISTRY_MODE=mock` + `MOCK_REGISTRY_FAIL_MODE=<mode>`）

| # | failMode | (a) 画面がクラッシュしない | (b) エラー種別が正しく出る | (c) 操作ログに残る | 担当 | 結果 | メモ |
|---|---|---|---|---|---|---|---|
| 2-1 | `timeout` | | | | | | |
| 2-2 | `5xx` | | | | | | |
| 2-3 | `reject` | | | | | | |
| 2-4 | `spec_mismatch` | | | | | | |
| 2-5 | `timeout_after_write` | | | | | | |

| # | 項目 | 期待 | 担当 | 結果 | メモ |
|---|---|---|---|---|---|
| 2-6 | (d) 更新系タイムアウト後の `info` 照合 | `timeout_after_write` で `renew` を落としたあと、最新化すると実際の状態に一致する | | | |
| 2-7 | (e) 参照系の自動再試行が最大 2 回で止まる | 操作ログに同一操作の試行が **3 行まで**（初回 + 再試行 2 回）で打ち止め（`READ_RETRY_COUNT = 2`） | | | |

### 2-B. 実レジストリの URL を無効化

`apps/api/.env.local` の `KITAQSIGN_BASE_URL` / `KITAQNIC_BASE_URL` を到達しない値にして起動する。

| # | 項目 | 期待 | 担当 | 結果 | メモ |
|---|---|---|---|---|---|
| 2-8 | (a) 画面がクラッシュしない | 一覧・詳細ともエラー表示に落ちるだけ | | | |
| 2-9 | (b) エラー種別が正しく出る | 「レジストリに接続できません」系のメッセージ（生のスタックは出さない。FR-18 / NFR-03） | | | |
| 2-10 | (c) 操作ログに記録される | `/logs` に失敗が残る | | | |
| 2-11 | 復帰 | URL を戻して「最新化」すると通常表示に戻る | | | |

## 3. 実レジストリでの全操作（必須 7 機能 + check + AuthCode）

**Kitaqsign と Kitaqnic の両方**で実施する（対応 TLD が重複しないので、両方を通さないと片方が未検証になる）。
テスト用ドメインは使い捨ての名前を使い、最後に廃止まで通す。

| # | 操作 | EPP | Kitaqsign（`.com` 等） | Kitaqnic（`.xyz` 等） | 担当 | メモ |
|---|---|---|---|---|---|---|
| 3-1 | 空き確認（FR-03） | `check` | | | | |
| 3-2 | 登録（FR-06） | `create` | | | | |
| 3-3 | 詳細参照（FR-07） | `info` | | | | |
| 3-4 | 更新（FR-08） | `renew` | | | | |
| 3-5 | 情報修正 NS（FR-09） | `update` | | | | |
| 3-6 | 情報修正 ロック（FR-09） | `update`（`add.statuses`） | | | | 8/27 の運営修正で反映されるようになった項目。D-02 のトグルから実施できる（#205） |
| 3-7 | 情報修正 コンタクト（FR-09） | `update` | | | | D-02 から実施できる（#172 で結線済み） |
| 3-8 | 廃止（FR-10） | `delete` | | | | |
| 3-9 | 復旧 RGP（FR-11） | `restore` | | | | |
| 3-10 | AuthCode 発行 | `rotate-auth-info` | | | | 発行のたびに値が変わる |

使ったドメイン名を記録しておく（後片付けと、当日デモで使い回さないため）:

| レジストリ | ドメイン名 | 最終状態 |
|---|---|---|
| kitaqsign | | |
| kitaqnic | | |

## 4. 本番 URL でのパスキー（FR-01）

本番ドメイン（`dopamin.ut42tech.com`）でしか確認できない（RP ID がドメインに紐づくため）。

| # | ブラウザ | サインアップ | ログアウト → ログイン | パスキー追加 / 削除 | 担当 | 結果 | メモ |
|---|---|---|---|---|---|---|---|
| 4-1 | Chrome | | | | | | |
| 4-2 | Safari | | | | | | |
| 4-3 | Edge | | | | | | |

| # | 項目 | 期待 | 担当 | 結果 |
|---|---|---|---|---|
| 4-4 | 未認証リダイレクト | 保護ルートを直接開くと `/login?next=...` に飛び、ログイン後に元の場所へ戻る | | |
| 4-5 | 最後のパスキーは削除できない | `LAST_PASSKEY` のエラーメッセージが出る | | |

## 5. 他チームとの相互移管（§21.2 #17）

**IN / OUT を各 1 回**。相手チームと時間を合わせて実施する。AuthCode の受け渡しは口頭 / チャットで。

### 5-A. 移管 IN（相手 → 自分）

| 項目 | 記録 |
|---|---|
| 相手チーム / レジストラ ID | |
| ドメイン名 | |
| AuthCode を受け取った日時 | |
| 申請した日時 | |
| 相手が承認した日時 | |
| 保有一覧に現れた日時 | |
| 結果 | |

### 5-B. 移管 OUT（自分 → 相手）

| 項目 | 記録 |
|---|---|
| 相手チーム / レジストラ ID | |
| ドメイン名 | |
| AuthCode を渡した日時 | |
| 申請を受信（Poll）した日時 | |
| 承認した日時 | |
| 保有一覧から消えた日時 | |
| 結果 | |

| # | 項目 | 期待 | 結果 | メモ |
|---|---|---|---|---|
| 5-1 | Poll 通知が届く | `/logs` に `poll` が残り、画面の移管一覧に出る | | |
| 5-2 | 履歴が残る | 完了後も `transfers` の行が履歴として残る | | |
| 5-3 | 出戻り | OUT 済みの同名ドメインを再び IN できる（部分一意インデックス） | | |

相手が確保できなかった場合は `mock` レジストリでの実演に切り替える（デモシナリオ 7 の代替）。
その場合はここに **「mock で代替」と実施日時**を明記すること。

## 6. デモシナリオ通し（§3.3 の 1〜9）

**本番 URL で通しで 1 回**。所要時間を測る（目標 5 分）。

| # | シナリオ | 期待 | 所要 | 結果 | メモ |
|---|---|---|---|---|---|
| 6-1 | パスキーでサインアップ | ユーザー名入力なしでログインできる | | | |
| 6-2 | 「たくたく」+「学生エンジニア」→ AI 候補 6 件 | 候補が 6 件出る | | | |
| 6-3 | 空き状況 + 独自性スコアの比較 | 有名サービスに似た候補が低スコアになる | | | |
| 6-4 | `.com` を Kitaqsign に登録 | お支払い画面で合計を確認 → 登録される（モック決済） | | | |
| 6-5 | GitHub リポ URL → サブドメイン提案 → DNS 反映 | 各ホストが `反映済み` になり NS が切り替わる | | | |
| 6-6 | 更新 → 廃止 → 復旧（RGP） | EPP ステータス遷移が画面に反映される | | | |
| 6-7 | 移管 IN（相手の承認 → 保有一覧に出る） | 5-A と同じ。相手が居なければ mock | | | |
| 6-8 | レジストリ通信エラー → 復帰 | メッセージ・操作ログ・再同期で復帰 | | | |
| 6-9 | 設定 →「開発者向け」→ ログを開く → AI ログ タブ | 開発中も含めた AI 利用が見える | | | |
| 6-合計 | | **5 分以内** | | | |

## 7. 後片付け

| # | 項目 | 結果 | メモ |
|---|---|---|---|
| 7-1 | §3 で作ったテストドメインを廃止した | | RGP 中の残留は許容（`docs/testing.md` §2） |
| 7-2 | `apps/api/.env.local` の `*_BASE_URL` を戻した | | 2-B で書き換えた場合 |
| 7-3 | `MOCK_REGISTRY_FAIL_MODE` を戻した | | |
| 7-4 | デモ用のデータを整えた（FR-16 のリセット） | | 当日は `POST /demo/reset` で初期化できる |

---

## 実施結果サマリ

| 節 | 実施日時 | 実施者 | OK / NG | 起票した issue |
|---|---|---|---|---|
| 1. 事前確認 | | | | |
| 2. エラーシミュレーション | | | | |
| 3. 実レジストリ全操作 | | | | |
| 4. パスキー | | | | |
| 5. 相互移管 | | | | |
| 6. デモシナリオ通し | | | | |
| 7. 後片付け | | | | |
