import { SettingsScreen } from "@/features/settings/settings-screen";

/**
 * S-70 設定 / S-71 リセット完了（Figma `85:6709` / `85:6884`）。
 * FR-01（テーマ・パスキー）/ FR-16（デモリセット）/ FR-17（AI 設定）。
 */
export default function SettingsPage() {
  return <SettingsScreen />;
}
